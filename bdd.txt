-- ######################################################
-- # PROYECTO GYM PRO - ESQUEMA ULTRA PRO              #
-- ######################################################

-- Actualización de la Tabla de Rutinas (Ultra Pro)
ALTER TABLE IF EXISTS gym_routines ADD COLUMN IF NOT EXISTS target_gender TEXT DEFAULT 'Unisex'; -- Masculino, Femenino, Unisex
ALTER TABLE IF EXISTS gym_routines ADD COLUMN IF NOT EXISTS target_age_group TEXT DEFAULT 'Adulto'; -- Niño, Joven, Adulto, Senior
ALTER TABLE IF EXISTS gym_routines ADD COLUMN IF NOT EXISTS health_conditions TEXT DEFAULT 'Ninguna'; -- Corazón, Asma, Diabetes, Lesiones, Ninguna
ALTER TABLE IF EXISTS gym_routines ADD COLUMN IF NOT EXISTS plan_duration_weeks INTEGER DEFAULT 4; -- 1 (Semanal), 4 (Mensual), 12 (Trimestral)
ALTER TABLE IF EXISTS gym_routines ADD COLUMN IF NOT EXISTS difficulty_level TEXT DEFAULT 'Intermedio'; -- Novato, Guerrero, Élite

-- Actualización de la Tabla de Recetas (Ultra Pro)
ALTER TABLE IF EXISTS gym_recipes ADD COLUMN IF NOT EXISTS prep_time TEXT DEFAULT '20 min';
ALTER TABLE IF EXISTS gym_recipes ADD COLUMN IF NOT EXISTS difficulty TEXT DEFAULT 'Media';
ALTER TABLE IF EXISTS gym_recipes ADD COLUMN IF NOT EXISTS diet_type TEXT DEFAULT 'Equilibrada'; -- Keto, Vegana, Proteica, Sin Gluten

-- ######################################################
-- # ESTRUCTURA DE TABLAS COMPLETA (PARA REFERENCIA)    #
-- ######################################################

/*
CREATE TABLE IF NOT EXISTS gym_recipes (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    ingredients TEXT,
    instructions TEXT,
    image_url TEXT,
    category TEXT,
    calories INTEGER,
    protein INTEGER,
    carbs INTEGER,
    fats INTEGER,
    prep_time TEXT,
    difficulty TEXT,
    diet_type TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS gym_routines (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    exercises JSONB DEFAULT '[]'::jsonb, -- Estructura Ultra: [{"week": 1, "days": [{"day": "Lunes", "moves": [...]}]}]
    level TEXT,
    goal TEXT,
    image_url TEXT,
    target_gender TEXT,
    target_age_group TEXT,
    health_conditions TEXT,
    plan_duration_weeks INTEGER,
    difficulty_level TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
*/

-- RLS (Habilitar si no lo están)
ALTER TABLE gym_recipes ENABLE ROW LEVEL SECURITY;
ALTER TABLE gym_routines ENABLE ROW LEVEL SECURITY;
ALTER TABLE gym_trainers ENABLE ROW LEVEL SECURITY;

-- Políticas Globales (Para desarrollo)
DROP POLICY IF EXISTS "Allow all for gym_recipes" ON gym_recipes;
CREATE POLICY "Allow all for gym_recipes" ON gym_recipes FOR ALL USING (true) WITH CHECK (true);

DROP POLICY IF EXISTS "Allow all for gym_routines" ON gym_routines;
CREATE POLICY "Allow all for gym_routines" ON gym_routines FOR ALL USING (true) WITH CHECK (true);
